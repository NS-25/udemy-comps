import React from "react";

const Table = ({ data }) => {
  const RenderRows = data.map(() => {
    
  })
  return (
    <table>
      <thead>
        <tr>Fruit</tr>
        <tr>Color</tr>
        <tr>Score</tr>
      </thead>
      <tbody>

      </tbody>
    </table>
  );
};

export default Table;
